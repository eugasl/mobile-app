import { Container, Row, Col } from "react-bootstrap";

import Layout from "../components/Layout";

const Info = () => (
  <Layout>
    <Container fluid>
      <Row>
        <Col>Info</Col>
      </Row>
    </Container>
  </Layout>
);

export default Info;
